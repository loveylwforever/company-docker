# 复制为 .env 后按需修改（在 company-docker 目录下）：
#   cp .env.example .env

POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

REDIS_PASSWORD=123456
REDIS_PORT=6379

MQ_USERNAME=admin
MQ_PASSWORD=Artemis@2026
ARTEMIS_CONSOLE_PORT=8161
ARTEMIS_CORE_PORT=61616

AUTH_PORT=8080
AUTH_CHANNEL_PORT=8090
ADMIN_DASHBOARD_PORT=3000

AUTH_SPRING_PROFILES_ACTIVE=prod
CHANNEL_SPRING_PROFILES_ACTIVE=prod

# amd64 离线构建（build-push-amd64.sh / docker-compose-amd64.yml）
# IMAGE_TAG=amd64
# NODE_HEAP_MB=2048
